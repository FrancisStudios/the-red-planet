export default class FSCanvasEngineValidator {
    /**
     * Validates if an object has keys (shallow)
     * @param {Object} targetObject 
     * @param {Array<string>} keys 
     * @returns {boolean}
     */
    validateObjectWithKeys(targetObject, keys) {
        return false;
    }
}
